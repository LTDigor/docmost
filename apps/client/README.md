# Docmost Client

React + TypeScript + Vite frontend for Docmost.

## Commands

```bash
pnpm install
pnpm --filter client dev
pnpm --filter client build
```

Use the root README and `AGENTS.md` for fork-level production notes.
